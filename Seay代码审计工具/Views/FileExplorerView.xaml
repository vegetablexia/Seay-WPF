<UserControl x:Class="Seay代码审计工具.Views.FileExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource BgLightBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="8,6" Background="{DynamicResource ControlBrush}">
            <TextBlock Text="文件浏览器" FontSize="11" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextDimBrush}"/>
        </Border>

        <TreeView x:Name="tvFiles" Grid.Row="1" 
                  Background="Transparent" BorderThickness="0"
                  Foreground="{DynamicResource TextBrush}"
                  MouseDoubleClick="tvFiles_MouseDoubleClick"/>
    </Grid>
</UserControl>
